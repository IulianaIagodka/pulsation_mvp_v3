/** Stable ids for session-persistent copy reveal (see `flow-copy-reveal.ts`). */
export const flowRevealIds = {
  triggerMain: "trigger-main",
  triggerPaths: "trigger-paths",
  triggerSpiralHint: "trigger-spiral-hint",
} as const;
